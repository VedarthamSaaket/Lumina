# backend/alembic.ini stub - run: alembic init alembic && alembic revision --autogenerate -m "init" && alembic upgrade head
# For quick start, the app auto-creates tables via SQLAlchemy on startup.
# For production, use Alembic migrations:
#   cd backend
#   alembic init alembic
#   # Edit alembic/env.py to import Base from models.database
#   alembic revision --autogenerate -m "initial"
#   alembic upgrade head
